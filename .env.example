# Optional Gmail SMTP (leave blank to skip email notifications)
SMTP_EMAIL=
SMTP_PASSWORD=
SMTP_RECEIVER=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Application Security
SECRET_KEY=change_this_to_a_random_secret_key
SESSION_TIMEOUT_MINUTES=30
